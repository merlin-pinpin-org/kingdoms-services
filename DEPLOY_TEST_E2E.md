# /deploy-test end-to-end check

This temporary file validates the on-demand test deployment flow:
the `/deploy-test` PR comment builds this PR's image and deploys it to
the test VPS. It is removed once the flow is verified.
